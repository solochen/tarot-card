<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.taluo.app">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".feature.tarot1.TarotAnimActivity">

        </activity>

        <activity android:name=".feature.tarot2.TarotAnimSwitchActivity" />

        <activity android:name=".feature.tarot3.TarotCircleCardActivity" />

        <activity android:name=".feature.pdfview.PdfViewActivity">

        </activity>

        <activity android:name=".feature.connection.FindGRActivity">

        </activity>

    </application>

</manifest>